import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//Aqui está o endereçamento do 'root' p/ o app.js, que é a função... funções!
ReactDOM.render(
  <React.StrictMode>
    <App />  
  </React.StrictMode>,
  document.getElementById('root'));
